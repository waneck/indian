package indian;

/**
	This is a special type that will build a wrapper around a pointer, so you
	can access the pointer's values as if it were a pointer to a struct
**/
@:genericBuild(some.pack)
class Struct<T>
{
}
